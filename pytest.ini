[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast isolated unit tests
    integration: requires network/keys/external services
    free_tier: free-tier guard tests
    rate_limit: rate limiter tests
    vcr: VCR cassette tests
